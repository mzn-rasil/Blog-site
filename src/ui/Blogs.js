

function Blogs({ children }) {
    return (
        <div className="container w-5/6 mx-auto mt-12 flex justify-start items-center">
            {children}
        </div>
    );
}

export default Blogs;